2+2
getwd()
vendas <- read.csv("Vendas.csv", fileEncoding = "windows-1252")
vendas <- read.csv("Vendas.csv", fileEncoding = "windows-1252")
setwd("C:\Users\rq_so\OneDrive\Área de Trabalho\Linguagem R - Análise Estatística (DSA)\Linguagem-R---Analise-Estatistica-DSA-")
getwd()
setwd("C:/Users/rq_so\OneDrive/Área de Trabalho/Linguagem R - Análise Estatística (DSA)/Linguagem-R---Analise-Estatistica-DSA-")
getwd()
setwd("C:/Users/rq_so/OneDrive/Área de Trabalho/Linguagem R - Análise Estatística (DSA)/Linguagem-R---Analise-Estatistica-DSA-")
getwd()
vendas <- read.csv("Vendas.csv", fileEncoding = "windows-1252")
View(vendas)
View(vendas)
str(vendas)
summary(vendas$Valor)
summary(vendas$Custo)
mean(vendas$Valor)
mean(vendas$Custo
?weighted.mean
weighted.mean(vendas$Valor, w = vendas$Custo)
weighted.mean(vendas$Valor, w = vendas$Custo)
median(vendas$Valor)
median(vendas$Custo)
resultado <- moda(vendas$Valor)
valor_unico <- unique(v)
moda <- function(v) {
valor_unico <- unique(v)
calor_unico(wich.max(tabulate(math(v, valor_unico))))
}
resultado <- moda(vendas$Valor)
print(resultado)
moda <- function(v) {
valor_unico <- unique(v)
calor_unico(wich.max(tabulate(math(v, valor_unico))))
}
resultado <- moda(vendas$Valor)
moda <- function(v) {
valor_unico <- unique(v)
valor_unico(wich.max(tabulate(math(v, valor_unico))))
}
resultado <- moda(vendas$Valor)
print(resultado)
# Moda
# Criando uma Função
moda <- function(v) {
valor_unico <- unique(v)
valor_unico(wich.max(tabulate(math(v, valor_unico))))
}
# Obtendo a Moda
resultado <- moda(vendas$Valor)
print(resultado)
moda <- function(v) {
valor_unico <- unique(v)
valor_unico(wich.max(tabulate(math(v, valor_unico)))]
}
moda <- function(v) {
valor_unico <- unique(v)
valor_unico(wich.max(tabulate(math(v, valor_unico))))
}
resultado <- moda(vendas$Valor)
resultado <- moda(vendas$Valor)
moda <- function(v) {
valor_unico <- unique(v)
valor_unico(wich.max(tabulate(math(v, valor_unico))))
}
moda <- function(v)
resultado <- moda(vendas$Valor)
print(resultado)
moda <- function(v) {
valor_unico <- unique(v)
valor_unico(wich.max(tabulate(math(v, valor_unico))))
}
# Obtendo a Moda
resultado <- moda(vendas$Valor)
print(resultado)
moda <- function(v) {
valor_unico <- unique(v)
valor_unico{wich.max(tabulate(math(v, valor_unico)))]
}
moda <- function(v) {
valor_unico <- unique(v)
valor_unico[wich.max(tabulate(math(v, valor_unico)))]
}
resultado <- moda(vendas$Valor)
moda <- function(v) {
valor_unico <- unique(v)
valor_unico[which.max(tabulate(math(v, valor_unico)))]
}
resultado <- moda(vendas$Valor)
moda <- function(v) {
valor_unico <- unique(v)
valor_unico[which.max(tabulate(match(v, valor_unico)))]
}
resultado <- moda(vendas$Valor)
print(resultado)
ggplot(vendas) +
stat_summary(aes(x = Estado,
y = Valor),
fun = mean,
geom = "bar",
fill = "lightgreen",
col = "grey50") +
labs(title = "Média de Valor Por Estado")
ggplot(vendas) +
stat_summary(aes(x = Estado,
y = Valor),
fun = mean,
geom = "bar",
fill = "lightgreen",
col = "grey50") +
labs(title = "Média de Valor Por Estado")
install.packages("ggplot2")
library(ggplot2)
library(ggplot2)
ggplot(vendas) +
stat_summary(aes(x = Estado,
y = Valor),
fun = mean,
geom = "bar",
fill = "lightgreen",
col = "grey50") +
labs(title = "Média de Valor Por Estado")
